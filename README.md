## prototype.solutions -=- https://discord.gg/gSSbbdBh3K


<h1 align="center">Quantuum Client loader decryptor (for the most recent builds)</h1>

**download deobfuscator: https://workupload.com/file/WUeQRNAQmx2** <br/>
password: **`nauseda`** <br/>
**`key.bin` and `key_scramble.json` are in the repo**

## usage:
put `key.bin` and `key_scramble.json` in the same folder as `quantumdeobfuscator.exe` <br/>
when it asks for the key either way use `vega33lineboostprototypesolutions` or `ai-made-loader//justbuyvegaline`

quantumdeobfuscator.exe [-h] [--p1] [--p2] [--p3] [--p4] [--p5] [--p6] [--p7] [--i] input_file <br/>

--p* is what payload you wanna save, 7 is the final decrypted one <br/>
--i includes either way everything or --p* and every single payload before that <br/>

also some decrypted loader variants included in the repository
